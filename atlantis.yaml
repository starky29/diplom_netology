version: 3
autoplan:
  when_modified: ["*.tf"]
  enabled: true
projects:
  - name: diplom_netology
    dir: .
    workflow: default
workflows:
  default:
    plan:
      steps:
        - run: terraform init
        - run: terraform plan
    apply:
      steps:
        - run: terraform apply